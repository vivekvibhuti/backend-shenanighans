// Package app provides HTTP routing configuration for the API.
//
// It defines all application routes, applies middleware chains, and maps
// handlers to their respective endpoints. Route definitions are centralized
// here to provide a clear overview of the API surface area.
package app