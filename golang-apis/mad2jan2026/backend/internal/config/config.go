// Package config handles application configuration management.
//
// It loads configuration from environment variables, config files, and
// defaults. All configuration values are validated and made available to
// the application through a centralized config struct.
package config