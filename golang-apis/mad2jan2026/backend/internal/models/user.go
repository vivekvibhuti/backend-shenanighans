// Package models defines domain data structures.
//
// Models are pure data types with no business logic. They represent entities,
// value objects, and request/response payloads used throughout the application.
// Validation is performed at the boundaries (handlers, services) to keep
// models simple and focused on data representation.
package models